

import 'package:flutter/material.dart';

class CuvedNavigationBar extends StatelessWidget {
  const CuvedNavigationBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
